{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@commitlint/config-conventional": "^17.1.0",
    "husky": "^8.0.1",
    "lerna": "^5.3.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test --"
  }
}
