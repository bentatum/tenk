export const cwd = process.cwd();
export const buildDir = `${cwd}/.tenk`;
export const assetsDir = `${buildDir}/assets`;
export const layersDir = `${cwd}/layers`;
export const configPath = `${cwd}/tenk.config.js`;
